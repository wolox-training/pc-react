import { StyleSheet } from 'react-native';

const avatarSize = 40;

const styles = StyleSheet.create({
  userAvatar: {
    height: avatarSize,
    width: avatarSize,
    borderRadius: 50
  }
});

export default styles;
